<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="demo.hc.com.f4">

    <uses-permission android:name="laiyu_send_owner" ></uses-permission>
    <permission
        android:name="laiyu_send_owner"
        android:protectionLevel="normal">

    </permission>

    <uses-permission android:name="laiyu_receive_owner" ></uses-permission>

    <permission
        android:name="laiyu_receive_owner"
        android:protectionLevel="normal">
    </permission>

    <application
        android:name=".Myapplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".MyReceiver"
            android:permission="laiyu_receive_owner">
            <intent-filter>
                <action android:name="laiyu_broatcast_owner"></action>
                <category android:name="receiver" />
            </intent-filter>
        </receiver>

    </application>
</manifest>